import { FilterScriptDevicePipe } from './filter-script-device.pipe';

describe('FilterScriptDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterScriptDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
