import { FilterSensorPipe } from './filter-sensor.pipe';

describe('FilterSensorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSensorPipe();
    expect(pipe).toBeTruthy();
  });
});
