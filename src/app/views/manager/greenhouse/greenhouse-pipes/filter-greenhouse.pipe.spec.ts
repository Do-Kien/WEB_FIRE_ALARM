import { FilterGreenhousePipe } from './filter-greenhouse.pipe';

describe('FilterGreenhousePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterGreenhousePipe();
    expect(pipe).toBeTruthy();
  });
});
